Our tweet-handler is responsible for reading over a collection of
tweets in a file format (e.g. json) and grouping them by similar
content, also allowing sub-filtering based on retweets.


Usage Instructions:
  Run tweet_handler.py from its source directory
